export default function AdminPage() {
  // TODO: user/job management — list users, manage roles, view ingestion and
  // report job status (admin-only)
  return (
    <div>
      <h1>Admin</h1>
    </div>
  );
}
