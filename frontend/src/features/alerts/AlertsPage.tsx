export default function AlertsPage() {
  // TODO: subscription CRUD UI — list, create, update, delete alert
  // subscriptions (min magnitude / region) against core-service
  return (
    <div>
      <h1>Alerts</h1>
    </div>
  );
}
