export default function DashboardPage() {
  // TODO: live earthquake feed — connect to core-service WebSocket and render
  // recent/incoming earthquake events in real time
  return (
    <div>
      <h1>Dashboard</h1>
    </div>
  );
}
