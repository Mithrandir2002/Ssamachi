export default function ReportsPage() {
  // TODO: report request form (date range) + list of past report requests
  // with status and download links, against core-service
  return (
    <div>
      <h1>Reports</h1>
    </div>
  );
}
